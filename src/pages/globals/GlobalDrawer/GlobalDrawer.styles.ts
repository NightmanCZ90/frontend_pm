import { styled } from "solid-styled-components";
import { Colors } from "../../../styles/theme";

interface IStyledGlobalDrawerProps {
  colors: Colors;
}

export const StyledGlobalDrawer = styled('div') <IStyledGlobalDrawerProps>`

`;